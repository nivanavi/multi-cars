import React from 'react';
import ReactDOM from 'react-dom/client';
import { MultiCarIgniter } from './App';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(<MultiCarIgniter />);
